"use client";import {usePathname} from "next/navigation";export default function PageTransition({children}:{children:React.ReactNode}){const p=usePathname();return <div key={p} className="pageTransition">{children}</div>}